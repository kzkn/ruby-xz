# frozen_string_literal: true

source 'https://rubygems.org'

gemspec

gem 'minitar', '~> 0.6'
gem 'rake', '~> 13.0'

if RUBY_VERSION >= '2.7.0'
  gem 'minitest', '~> 5.25.5'
else
  gem 'minitest', '< 5.16.0'
end
